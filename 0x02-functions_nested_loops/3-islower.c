#include "main.h"
/**
 * -islower - checks for case character
 *  @c: the character to be checked
 *  Return: 1 if c is a letter, 0 otherwise
 */
int _islower(int c)
{
	return ((c >= 'a' && c <= 'z');
}
