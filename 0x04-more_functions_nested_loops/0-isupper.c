#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: the character to be checked
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
